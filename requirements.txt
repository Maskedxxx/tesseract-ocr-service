# Зависимости OCR Service v2
#
# Единый контейнер — все зависимости в одном месте.
# Установка: pip install -r requirements.txt

# === Web Framework ===
fastapi==0.109.0
uvicorn==0.27.0
python-multipart==0.0.6

# === Validation ===
pydantic==2.5.3
pydantic-settings==2.1.0

# === PDF Processing ===
pdf2image==1.16.3
# Требует poppler-utils на системе (установлен в Docker)

# === Image Processing ===
Pillow==10.2.0
numpy==1.26.3

# === OCR ===
pytesseract==0.3.10
# Требует tesseract-ocr на системе (установлен в Docker)

# === Deskew (коррекция наклона) ===
deskew==1.4.0
